@* @{
  Layout = "_Layout";
}

@using University.Models;
@model List<University.Models.Department>;

<h1>Departments</h1>

@if (@Model.Count == 0)
{
<h3>No dpartment has been added yet</h3>
}

@foreach (Department department in Model)
{
  <li>@Html.ActionLink($"{department.DepartmentName}","Details", new { id = department.DepartmentId})</li>
}

<p>@Html.ActionLink("add a Department", "Create")</p>
<p>@Html.ActionLink("home", "Index", "Home")</p> *@